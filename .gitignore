# Build output. Published to gh-pages, never committed to the default branch.
/dist/

# Build intermediates and downloaded tools: the pmtiles binary, the Python venv,
# the vendored asset checkout. All re-fetched by build.sh on a clean checkout.
/.work/

node_modules/

# Never commit any of these, on either branch. They are hundreds of megabytes
# and are byproducts, not sources.
*.osm.pbf
*.mbtiles
*.pmtiles
planet*.pmtiles

.DS_Store
